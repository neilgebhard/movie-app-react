import styles from "./Footer.module.css";

const Footer = () => {
  return (
    <section className={styles.footer}>
      <div className="container">Neil's Movie App</div>
    </section>
  );
};

export default Footer;
